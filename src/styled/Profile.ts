import styled from '@emotion/styled';

export default styled('div')`
  display: flex;
  flex-direction: row;
  text-transform: capitalize;

  > img {
    width: 100px;
    height: 100px;
    margin-right: 1em;
  }
`;
